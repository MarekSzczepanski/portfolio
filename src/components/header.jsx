import * as React from 'react';

function Header() {
  return (
    <header
      style={{
        margin: `0 auto`,
        display: `flex`,
        alignItems: `center`,
        justifyContent: `space-between`,
      }}
    />
  );
}

export default Header;
